// Copyright IBM Corp. 2023, 2024
import wxflows from './lib/wxflows'
export default wxflows

// Hack: support both ESM-style imports: `import wxflows from 'wxflows'`
// and keep the backwards-compat with CJS-style imports: `const wxflows = require('wxflows')`
// NOTE: if at any point in the future there is a name collision between `wxflows` and ES exports
// this hack will break
module.exports = wxflows
